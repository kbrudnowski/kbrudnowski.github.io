# kbrudnowski.github.io

Personal CV / resume site — live at **https://kbrudnowski.github.io**.

Single self-contained `index.html`. No build step, no dependencies.

To generate a PDF: open in a browser and use File → Print → Save as PDF (or click the Download PDF button on the page). The print stylesheet produces a clean A4 layout.
